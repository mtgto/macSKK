# プライベートモード

プライベートモードが有効なときは変換結果がユーザー辞書に記録されません。

## ONのときの動作

- ユーザー辞書以外の辞書は参照される
- プライベートモードを有効にする前のユーザー辞書の変換候補は参照される
- 新しい変換結果はユーザー辞書に保存されない

## 有効化・無効化

**入力メニュー → プライベートモード** から切り替えます。

## 関連設定

- [一般](../config/general#プライベートモード) — プライベートモード中にユーザー辞書を無視するかの設定
