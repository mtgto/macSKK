# 数値変換

辞書に `#` を含む読みと変換候補があるエントリは、数値変換エントリとして扱われます。

## 書式

```
だい# /第#0/第#1/
```

読みに `#`、変換候補に `#(数字)` を含む形式です。

## 対応タイプ

macSKKはタイプ0, 1, 2, 3, 8, 9に対応しています。数値として使えるのは `0` 以上 `2^63-1` (Int64.max) までです。

## ユーザー辞書への記録

数値変換エントリを使って変換した場合、ユーザー辞書には実際の入力数値を含まない形式で保存されます。
