// SPDX-License-Identifier: GPL-3.0-or-later

import SwiftUI

// 日付変換の読み編集画面。
// 読みは現在時間からの差分も設定できる (例えば "きのう" で変換したら昨日の日付をベースにするとか)
struct DateYomiView: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

#Preview {
    DateYomiView()
}
